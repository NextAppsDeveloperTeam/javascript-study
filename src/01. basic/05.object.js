const ll = console.log;

// TODO: { id: 1, name: 'kim' } 으로 변경 (name 값 변경)
{
  const obj = { id: 1, name: 'park' };
}

// TODO: { id: 1, name: 'park', tel: '010-1234-5678' } 로 변경 (항목 추가)
{
  const obj = { id: 1, name: 'park' };
}

// TODO: 주어진 key, value 로 값 변경. { id: 1, name: 'kim' }
{
  const obj = { id: 1, name: 'park' };
  function setValue(key, value) {
    // 이곳에 코드 작성
  }
  setValue('name', 'kim');
}

// TODO: 모든 key 배열로 구하기. ['id', 'name']
{
  const obj = { id: 1, name: 'park' };
}

// TODO: 모든 value 값 합산. key 를 모른다고 가정해야함
{
  const obj = { n1: 323, n2: 553, n3: 234, n4: 553, n5: 952 };
}
